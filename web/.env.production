# 生产环境变量（npm run build 时自动加载）
# Vercel 部署时实际值会在 Vercel 后台环境变量里覆盖；
# 这里写一个占位提醒部署时改
#
# 部署到 Vercel 后：
#   1. 在 Vercel 项目设置 → Environment Variables 加 VITE_API_BASE_URL
#   2. 值 = Railway 给出的后端域名 + /api，如 https://mini-crm-api.up.railway.app/api
#   3. 重新 deploy 才生效
VITE_API_BASE_URL=https://your-railway-domain.up.railway.app/api
