# 生产环境变量（npm run build 时自动加载）
#
# ⚠️ Vercel 部署时，Vercel 后台配置的 Environment Variables 会**覆盖**这里的值
#    如果 Vercel 后台没配 VITE_API_BASE_URL，那 Vercel build 时用的就是下面这个
#
# 迁 Cloudflare Workers（feat/cf-workers 分支）：
#   原 Render：https://mini-crm-api-mr3b.onrender.com/api
#   过渡 Workers 默认域：https://mini-crm-api.2403400717aaa.workers.dev/api（大陆访问不稳）
#   现在 CF 自定义域：https://api.bobodylan.com/api（DNS + IP 都走 CF，大陆稳定）
#
# 备注：mini-crm-api.2403400717aaa.workers.dev 这个 fallback 域名也还在跑，
#      自定义域真出问题可以立刻切回来
VITE_API_BASE_URL=https://api.bobodylan.com/api
