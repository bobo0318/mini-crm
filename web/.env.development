# 开发环境变量（npm run dev 时自动加载）
# 这个文件可以 commit 进 git —— 里面不该有任何密钥
#
# baseURL 设成 /api：让请求走 Vite proxy 转发到本地后端 :3000
# （vite.config.ts 里的 server.proxy['/api'] -> http://localhost:3000）
VITE_API_BASE_URL=/api
