# 前端环境变量模板
# 复制此文件为 .env.local（本地敏感配置）或直接用 .env.development / .env.production
#
# Vite 约定：只有 VITE_ 前缀的变量才能在浏览器代码里通过 import.meta.env.VITE_XXX 读到

# 后端 API 的 base URL（不带末尾斜杠）
# 开发：/api（走 Vite proxy）
# 生产：完整后端域名 + /api，如 https://your-api.railway.app/api
VITE_API_BASE_URL=/api
