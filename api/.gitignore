# 依赖包目录（巨大，靠 npm install 重新生成）
node_modules/

# SQLite 数据库文件（本地数据，不同环境不一样，永远别 commit）
# 包含 SQLite 的 journal/wal/shm 临时文件
*.db
*.db-journal
*.db-wal
*.db-shm

# 环境变量（敏感，比如 JWT secret、数据库密码）
.env
.env.local
